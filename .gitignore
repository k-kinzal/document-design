# Dependencies and generated output
node_modules/
dist/
# The report action's distribution is committed: consumers run it with
# `uses:` and no install step. A test keeps it in sync with the source.
!packages/doc-report/dist/
storybook-static/
pages/
.generated/
.generated-dev/

# Test output
test-results/
playwright-report/
coverage/

# Local configuration; keep shareable examples
.env
.env.*
!.env.example
!.env.*.example

# Local system and tool files
.DS_Store
*.log
