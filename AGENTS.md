# AGENTS

## Vision

k-kinzalの作るドキュメント、ペーパーサイト用のデザインシステムを作成します。
情報密度、視覚情報の視認性を重視して情報をどう見やすくするのかにフォーカスしたデザインを提供します。

## Architecture

### Design System

#### 何を相手にしているか

主題は **プログラムが書き、アーカイブされる文書** です。ここから設計のほとんどが決まります。

- **誰も来たくて来ていない。** ツールが吐いたページに、リンク経由で、ひとつの問いを持って途中から入る
- **量は無編集で青天井。** 844 statements を誰も選別していない。200行の1件と3行の1件が同じリストに並ぶ
- **セッションではなく成果物。** アーカイブされ、メールで送られ、印刷され、2年後に `file://` で開かれる
- **文字列連結で書かれる。** PHP / Rust / シェルが生成する。**HTMLの形がそのまま公開APIである**

アプリのUIではありません。装飾は情報を増やさず、印刷でインクを食い、5年後に時代の刻印になります。

#### 原則（全文は Storybook の Foundations → Principles）

1. **読者は問いを持って来ている。** 「今どこにいるか」と「他にどこへ行けるか」を毎ページ答える
2. **わからないことを、わからないと言えること。** ← 他が全部これに従う<br>
   解決できなかった解析器は何かを学んでいる。成功しか描けないデザインは生成側に切り上げをさせ、下限が合計として印字され、文書は自信を持って間違う。だから不在は一級の表現（`.hole` `.meter-part.is-open` `.empty` `.caveat`）であり、**赤は他の用途に一切使わない**（構文強調にも種別にも使わない。ページ上の唯一の赤が常に「ここを見ろ」を意味するため）
3. **数字は問いを伴う。** `978` ではなく `978 findings`。カウントは単独で置かない
4. **2つの読み方＝2つの速度。** カタログは走査（密度）、レポートは通読（余白）。1つのスケールでは両立しない
5. **色は語彙であって装飾ではない。** 識別色（何であるか／無評価）と状態色（どうなっているか）を混ぜない。混ぜるとカタログが警報盤になる
6. **すべての印は二重に言う。** 色だけで区別しない。diffは `+`、ファセットは枠線、現在地はバー。理由の大半は**印刷するとtintが消える**から
7. **ページは道具より長生きする。** ビルド不要・JS不要・ネットワーク不要。印刷は正式な出力先
8. **密度を視力で買わない。** 文字を小さくして詰めるのではなく、余白を削って詰める

#### 技術的にどう実現しているか

| 課題 | 手段 | なぜそれか |
|---|---|---|
| 誰が勝つか | `@layer dd.{reset,tokens,base,layout,component,utility,print}` | 命名規約は各ジェネレータが守り続ける約束。層はブラウザが守る規則。**これがクラス名を素のままにできる根拠** |
| 名前の衝突 | カスタムプロパティのみ `--dd-` | `--*` は文書全体を継承で貫き、`@layer` も `@scope` も止められない。かつテーマAPIなので安定名が要る |
| テーマ | `light-dark()` + `color-scheme` | パレットを1回だけ書く。手動切替も `color-scheme` を変えるだけ |
| tint | `color-mix(in oklab, hue var(--dd-tint-mix), bg)` | 手書きの20色×2テーマは20箇所の破綻余地。`--dd-tint-mix` は**コントラスト目標から逆算**した値であって好みではない |
| 主張の検証 | `scripts/contrast.mjs`（CI必須、150ペア） | 「アクセシブル」は検査できて初めて主張になる |
| 色の直交 | `--dd-tone` / `--dd-tone-tint` を各コンポーネントが読む | 種別が増えてもコンポーネントは増えない。`.k-select` 等は利用側が1行でエイリアス |
| 幅への応答 | コンテナクエリ（**無名**＝最近傍） | 1400pxの窓に320pxの列がありうる。ジャンルを入れ子にできる約束を守るため |
| 長大リスト | `[data-dd-defer]` → `content-visibility` | 844行の初期描画。未対応でも見た目は不変 |
| 振る舞い | data属性駆動の**クラシックスクリプト**（任意） | ESモジュールは `file://` で失敗する。JSを外しても何も壊れない |
| 印刷 | 独立した最終層 `dd.print` | 画面都合で隠したものは紙では開けない。フィルタ済み行・閉じた details・非活性タブは全部開く |

#### 触るときの約束

- **`@scope` を基盤に使わない。** Baseline入りは2026年1月。未対応環境では at-rule ごと破棄され、アーカイブ文書が**完全に無スタイル**になる。劣化ではなく全崩壊
- **`!important` は2箇所だけ。** `dd.reset` の `prefers-reduced-motion` と `dd.print`。どちらも利用側にも勝つべきもの。それ以外に書いたら設計を間違えている
- **ジャンル変種はコンポーネント層に置く。** 層の順序は詳細度より**先に**決まるので、`dd.layout` に書いた `.sheet .stat` は `dd.component` の既定に負ける
- **チップも4.5:1。** WCAGの大文字例外は18.66px bold から。12px/600 は通常テキスト
- **数字は折らない。** `.fig` `.stat-fig` は `overflow-wrap: normal`。折れた数字は小さい数字ではなく別の数字
- **クラス名を長くしない。** `.tok-kw` は1ページに数千回出る。生成物のバイト数は読者の密度に効くが、BEM的な長名は効かない
- **レビュー提案はそのまま当てない。** 詳細度や `min()` の向きを外した提案が実際に複数あった。**ブラウザで再現→修正→再計測**が手順

### Product Page

TBD

## Examples

元になるデザインのサンプルです。これらに適用するためのものであり既存のデザインを踏襲する必要はありませんが、よりよいデザインにする必要があります。

- [PHP DocGen](https://github.com/k-kinzal/php-ai-toolkit)
    - https://k-kinzal.github.io/ztd-query-php/pr/393/
- [SQL Catalog](https://github.com/k-kinzal/ztd-query-php)
    - /Users/ab/Desktop/wordpress-sql-catalog/index.html
- [QuuuAI](https://github.com/k-kinzal/quuu)
    - /Users/ab/Library/Application                            
  Support/taskd/reports/tsk_bc4869e9ddf54c998b18/rpt_2f13da82c39248859bc8.html